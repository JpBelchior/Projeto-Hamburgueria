// GASTO_INGREDIENTE — compra de ingredientes registrada manualmente

model GastoIngrediente {
  id            Int         @id @default(autoincrement())
  nome          String      @default("")
  valor         Float
  descricao     String?
  mes           Int
  ano           Int
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt

  restauranteId Int
  restaurante   Restaurante @relation(fields: [restauranteId], references: [id])
  ingredientes  GastoIngredienteIngrediente[]

  @@map("gasto_ingredientes")
}

model GastoIngredienteIngrediente {
  gastoIngredienteId  Int
  ingredienteId       Int
  quantidade          Float    @default(0)

  gastoIngrediente    GastoIngrediente @relation(fields: [gastoIngredienteId], references: [id], onDelete: Cascade)
  ingrediente         Ingrediente      @relation(fields: [ingredienteId], references: [id])

  @@id([gastoIngredienteId, ingredienteId])
  @@map("gasto_ingrediente_ingredientes")
}
